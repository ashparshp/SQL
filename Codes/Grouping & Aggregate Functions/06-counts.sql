-- SELECT COUNT (*) AS total_bookings FROM bookings;

-- SELECT COUNT (booking_date) AS total_booking_date FROM bookings;

-- SELECT COUNT (amount_tipped) FROM bookings;

-- SELECT COUNT (DISTINCT booking_date) FROM bookings;