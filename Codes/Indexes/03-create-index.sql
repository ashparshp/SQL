-- CREATE INDEX idx_salary ON users (salary);

-- CREATE UNIQUE INDEX idx_salary ON users (salary); -- Unique index to ensure no duplicate salaries it is same as using UNIQUE constraint in table definition for salary column...

-- DROP INDEX idx_salary;